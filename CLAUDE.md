# CLAUDE.md — cobot3_ws

> 공통 규칙은 `~/.claude/CLAUDE.md`에 있다.
> **이 ws는 라이브 개발 사본이다.** 포트폴리오 제출용 스냅샷은 물리적으로 별개인 `~/vault/portfolio/sources/cobot3`(실체: 외장볼륨, git 아님, 읽기 전용)에 있다 — 이름이 같다고 같은 파일이 아니다. "포트폴리오/자소서" 맥락 없이 코드·문서 수정 요청이 오면 이 ws(라이브)를 고치는 게 맞다.
